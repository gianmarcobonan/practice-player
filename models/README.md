# models/

Modelli ONNX per la separazione delle tracce (popolati nella fase stem).

- `htdemucs_ft.onnx` (o equivalente) — separazione in 4 stem: voce, batteria, basso, altro.

Scaricato una volta e incluso nella build portable.
