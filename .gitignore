# Dependencies
node_modules/

# Build output
dist/
src/renderer/dist/
.bin-tmp/

# Bundled native tools (fetched via scripts/fetch-binaries.mjs; ffmpeg > 100MB).
# Windows (.exe) and Linux binaries; only the README stays in git.
/bin/*
!/bin/README.md

# VC++ Redistributable (fetched via scripts/fetch-binaries.mjs) bundled into
# the Windows NSIS installer via build/installer.nsh.
/build/vc_redist.x64.exe

# Runtime/user data (models cache, downloads, settings, stem cache, projects)
data/

# Misc
*.log
.DS_Store
Thumbs.db
