# Dependencies
node_modules/

# Build output
dist/
src/renderer/dist/
.bin-tmp/

# Bundled native tools (fetched via scripts/fetch-binaries.mjs; ffmpeg > 100MB).
# Windows (.exe) and Linux binaries; only the README stays in git.
/bin/*
!/bin/README.md

# Runtime/user data (models cache, downloads, settings, stem cache, projects)
data/

# Misc
*.log
.DS_Store
Thumbs.db
